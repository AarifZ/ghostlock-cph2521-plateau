# HANDOFF — 2026-09-12 (CVE-2026-43499 → usable root on CPH2521)

> **Pickup:** `docs/NEXT_SESSION_2026-09-12.md`  
> **GLM pickup:** `docs/GLM_HANDOFF_PICKUP_2026-09-12.md`  
> **ColorOS popup:** `docs/COLOROS_POPUP_VS_ROOT_2026-08-29.md`  
> 08-29 / 09-01 handoffs are history. Next-fire is this file’s session.

---

## Where we are in one paragraph

GhostLock’s UAF+pselect+rb_erase AAW is on-device on OPPO Reno 10 Pro+ **CPH2521** (kernel `5.10.236-android12-9-o-g74d132f4467a`, locked BL, ColorOS 16.0.5.1002 EX01, slot `_b`). We can leak KASLR, park SELinux Permissive, unhook `sys_exit` ROOTGUARD, leak `task_struct` via user-PMU x28, and we once logged a **child** `/proc/status` uid 0 for ~2 minutes (Z33, 2026-08-24). On **2026-09-12** the operator asked to fire Exact Z33 (park + child `+0x780` even on `ffffff88/89` x28). W1 park landed; W2 `*ffffff88bb2cac80=init_cred` **kernel-panicked** at `pselect pre-select +2ms`. `/data/local/tmp/ROOTED` survived as 25 NUL bytes owned by root — **not** a living root shell. JoinChang stock Path A missed (`success=0`). We still do not have an ADB root shell, KernelSU, or unlimited kR/W. Recommended next is **same-process hookcred after a self-only punch gate**, not a blind Z33 retry, not two-process `gl_run_chain.sh`.

---

## This session’s operator thread (short)

- Why stop at Permissive? Permissive ≠ uid0.
- JoinChang README / issue #31 / ROOTGUARD / Aristotle / Z Fold / GLM “one step away”: CPH is offsets-extracted, not a verified JoinChang win. Path B W1 is our banned 8-byte selinux store.
- Picked fire **1** (Z33+child payload), then Exact Z33 over P0-only HOLD.
- Exact Z33 KPd. Asked next plan. Offered A hookcred self-only / B retry Z33 / C park-only. Then: hand off + push `research-master`.

---

## Proven (do not re-litigate)

Unchanged from 08-29: compact waiter; P0 AAW; spray-free SLIDE; PLAIN park; SWAP_NOCFI never-open; `change_child` clobber; UMH dead (`STATIC_USERMODEHELPER_PATH=""`); ROOTGUARD = `oplus_security_guard` on `sys_exit`; leaf-NULL unhook lives; user-PMU x28; walk budget ~2/boot; second process after a walk KPs.

New this session:

13. **Exact Z33 on a high-alias child is not free.** 08-24 WIN vs 09-12 KP at the cred `pselect`. Do not wrap the alias (`ffffff807b2ca500` was logged and correctly unused).
14. **Child auto-payload did not get a living G.** `ROOTED` 25 NULs + `uid0_id.txt` gid=`0xBB2CA500` is panic debris (pointer-bit gid, same family as Z33’s dirty suid/gid).
15. **JoinChang stock Path A** on this phone: `pselect success=0` step=33. Path B W1 success=0 and banned.
16. **Hookcred as written will punch the child** when self x28 is not P0. Must gate before fire.
17. Cred stamp default is back to **PLAIN only-left**. `MODE4_CRED_RIGHT=1` is the GLM only-right that KPd 4/4.

---

## Dead / banned (additions)

| What | Why |
|------|-----|
| Blind Exact Z33 retry | 09-12 KP at W2 pselect |
| `gl_run_chain.sh` P2 after live P1 | Z5 two-process |
| JoinChang Path B W1 | 8-byte selinux |
| Wrap child `ffffff88` → `ffffff80` | Z23/Z28 |
| Treat `ROOTED` 25 NULs as uid0 | not ASCII; no WIN line |
| Fire hookcred before self-only gate | falls through to today’s KP target |
| Dual USB+WiFi exec | `error: closed` |

---

## Tree vs `743669e`

```
M  src/core/fops.c           PLAIN only-left cred; CRED_RIGHT opt-in;
                             child retry=1; getuid==0 proof file
M  src/core/main.c           PREFER_CHILD; auto-G on status Uid 0;
                             ROOTED + uid0_go; UID0_SLOT
M  src/core/util.c           slots 0x780/0x788/0x778; self getuid query
M  ghostlock-cph2521         226752  md5 46c058e3269e9d1a9fd8384f84cb0aea
M  docs/DEVICE_ADB_NET.md    WiFi 192.168.1.2
A  gl_run_parkchild.sh       Exact Z33
A  gl_run_root.sh            MODE4_ROOT HOLD — do not fire casually
A  gl_run_chain.sh           two-process — do not use after live W1
A  watch_adb_wifi.ps1
A  docs/*2026-09-12.md
A  logs/Z33_parkchild.txt    KP console (ANSI + trailing NULs)
```

Tracked already: `gl_run_hookcred.sh` `gl_run_uid0.sh` `gl_run_holdpark.sh` `gl_run_slide.sh` `gl_run_swap.sh` `gl_run_zi.sh` (ZI banned) `gl_run_gboot.sh` `gl_run_kptr.sh` `gl_run_tpnull.sh` `gl_run_repair.sh`.

---

## Device / ADB

| | |
|--|--|
| WiFi | `192.168.1.2:5555` Shizuku — **all exploit** |
| USB | `596666e9` — `tcpip 5555` bootstrap only |
| Host | `adb_local.exe` in this repo, or WinGet platform-tools |
| Last boot classified | `c2773f39` Enforcing uid 2000 (re-classify) |
| StdSP | still disabled-user |

---

## Success / honesty

- `UID0 WIN child is root` happened **once** (Z33 08-24). It is not product root and the 09-12 copy KPd.
- Do not write parent `getuid=0` unless `/data/local/tmp/uid0_id.txt` or console shows `getuid_after_store=0` as **ASCII**.
- Product root = ADB `id` uid=0 **or** a world-readable ASCII proof **and** the boot still up after 3+ minutes **and** a persist story.
